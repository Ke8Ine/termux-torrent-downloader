# termux-torrent-downloader
A termux bash script that downloads the current file when opened.

### Installation
Create a folder with a name `bin` in your root folder or $HOME.

Then move the `./script` to the `bin/` folder.
